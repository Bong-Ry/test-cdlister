<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>解析結果 - CD出品アシスタント</title>
  <style>
    body { font-family: sans-serif; }
    .container { max-width: 800px; margin: auto; padding: 20px; }
    .form-group { margin-bottom: 15px; }
    label { display: block; font-weight: bold; margin-bottom: 5px;}
    input[type="text"], input[type="number"], textarea { width: 100%; padding: 8px; box-sizing: border-box; }
    textarea { height: 100px; }
    pre { background-color: #f4f4f4; padding: 15px; border-radius: 5px; white-space: pre-wrap; word-wrap: break-word; }
  </style>
</head>
<body>
  <div class="container">
    <h1>解析結果</h1>

    <div class="form-group">
      <label for="title">タイトル</label>
      <input type="text" id="title" name="title" value="<%= result.Title %>">
    </div>

    <div class="form-group">
      <label for="price">価格</label>
      <input type="number" id="price" name="price" value="">
    </div>

    <div class="form-group">
      <label for="shipping">送料</label>
      <input type="number" id="shipping" name="shipping" value="210">
    </div>

    <div class="form-group">
      <label for="comment">コメント</label>
      <textarea id="comment" name="comment"></textarea>
    </div>

    <hr>

    <h2>商品説明 (HTML)</h2>
    <pre id="description">
【Disc Condition】: new
【Case Condition】: new
【Booklet/Insert】: new
【OBI Strip】: new
&lt;br&gt;
&lt;br&gt;
<% if (result.isFirstEdition || result.hasBonus) { %>
--- 特典・エディション情報 ---
<%= result.editionNotes %>
&lt;br&gt;
&lt;br&gt;
<% } %>
--- 収録曲 ---
<%= result.Tracklist.replace(/, /g, '&lt;br&gt;') %>
    </pre>

    <hr>

    <h2>AIによる解析データ (確認用)</h2>
    <pre><%= JSON.stringify(result, null, 2) %></pre>

  </div>
</body>
</html>
